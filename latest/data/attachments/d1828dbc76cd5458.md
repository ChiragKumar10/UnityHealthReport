# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: provider-portal/patient-wallet/cpt-99457-99458x2-rpm-60min-qualification.spec.ts >> Interaction Billing (RPM) — CPT 99457 + 99458×2 Qualification (60 min) >> care coordinator logs 60 min on RPM — patient qualifies for 99457 + 99458 (≥ 2 increments)
- Location: tests/web/provider-portal/patient-wallet/cpt-99457-99458x2-rpm-60min-qualification.spec.ts:145:9

# Error details

```
Error: Patient row for Block, Micheal should appear in Billing > Interaction > RPM after logging 60 min

expect(locator).toBeVisible() failed

Locator: locator('//*[normalize-space(text())="Block, Micheal"]/ancestor::*[count(*) >= 5][1]').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Patient row for Block, Micheal should appear in Billing > Interaction > RPM after logging 60 min with timeout 15000ms
  - waiting for locator('//*[normalize-space(text())="Block, Micheal"]/ancestor::*[count(*) >= 5][1]').first()

```

```yaml
- banner:
  - img "Logo"
  - combobox "Search Patient Name, DOB (MM-DD-YYYY), Phone"
  - button "Open"
  - button "New Patient":
    - paragraph: New Patient
  - button
  - button
- list:
  - listitem: Virtual Services
  - list:
    - listitem: Care Team Work List
    - listitem: Enrollments
    - listitem: Patient Assessments
    - listitem: Alerts
    - separator
    - listitem: Tasks 164
    - listitem: Communications
    - separator
    - listitem: Billing
    - separator
    - listitem: Reports
    - separator
    - listitem: Document Library
    - listitem: CCM Library
    - listitem: PCM Library
    - listitem: RPM Library
    - listitem: RTM Library
    - separator
    - listitem: Training Library
  - listitem: Call Center
  - listitem: Revenue Cycle Management
  - listitem: Omni Channel
  - separator
  - listitem: Settings
- paragraph: Jemes Cory
- list:
  - listitem: Logout
- listitem
- main:
  - heading "Billing" [level=6]
  - button "Choose date, selected date is Aug 1, 2026"
  - textbox "Select Month": Aug 2026
  - text: Enrollments
  - table "simple table":
    - rowgroup:
      - row "RPM RTM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
    - rowgroup:
      - row "To Be Billed 112 635":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "112":
          - paragraph: "112"
        - cell "635":
          - paragraph: "635"
      - row "Submitted 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - text: Monitoring
  - table "simple table":
    - rowgroup:
      - row "RPM RTM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
    - rowgroup:
      - row "To Be Billed 0 0":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
      - row "Submitted 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - text: Interaction
  - table "simple table":
    - rowgroup:
      - row "RPM RTM CCM PCM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
        - columnheader "CCM":
          - paragraph: CCM
        - columnheader "PCM":
          - paragraph: PCM
    - rowgroup:
      - row "To Be Billed 0 15 0 0":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "0":
          - paragraph: "0"
        - cell "15":
          - paragraph: "15"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
      - row "Submitted 0 0 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - heading "INTERACTION (RPM)" [level=5]
  - textbox "Search Patient": Block
  - textbox "Search By Insurance"
  - checkbox "SelfPay"
  - text: SelfPay
  - combobox "Select": All Providers
  - button "Open"
  - combobox: Claim Status
  - combobox: Paid Status
  - button "Export Billing"
  - checkbox "select all rows"
  - text: "NAME ▲ SERVICE CRITERIA # TIME DIAGNOSIS CODES(ICD) PROVIDER CPT CODE INSURANCE CLAIM NUMBER CLAIM STATUS PAID DATE PAID STATUS ACTION"
  - heading "No data found." [level=6]
- button
- button
```

# Test source

```ts
  96  |     await page.waitForLoadState('networkidle').catch(() => { /* noop */ });
  97  | }
  98  | 
  99  | async function gotoBillingDirect(page: Page): Promise<void> {
  100 |     Logger.step('Navigate to /provider/billing via direct URL');
  101 |     const origin = new URL(page.url()).origin;
  102 |     page.once('dialog', (d) => d.accept().catch(() => { /* noop */ }));
  103 |     await page.goto(`${origin}/provider/billing`, { waitUntil: 'domcontentloaded', timeout: 30000 });
  104 |     await page.waitForLoadState('networkidle').catch(() => { /* noop */ });
  105 |     await expect(page.getByText(/^Billing$/, { exact: true }).first())
  106 |         .toBeVisible({ timeout: 15000 });
  107 | }
  108 | 
  109 | async function pickBillingMonth(page: Page, monthYear: string): Promise<void> {
  110 |     Logger.step(`Set Billing month filter to "${monthYear}"`);
  111 |     const monthInput = page.getByPlaceholder('Select Month').first();
  112 |     if (!(await monthInput.isVisible({ timeout: 5000 }).catch(() => false))) {
  113 |         Logger.info('Month filter input not visible — skipping');
  114 |         return;
  115 |     }
  116 |     await monthInput.click({ clickCount: 3 });
  117 |     await monthInput.fill(monthYear);
  118 |     await monthInput.press('Enter');
  119 |     await page.waitForLoadState('networkidle').catch(() => { /* noop */ });
  120 | }
  121 | 
  122 | async function selectBillingInteractionRpm(page: Page): Promise<void> {
  123 |     Logger.step('Click "RPM" cell under "Interaction" section in Billing');
  124 |     const cell = page
  125 |         .locator('xpath=//*[normalize-space(text())="Interaction"]/following::*[normalize-space(text())="RPM"][1]')
  126 |         .first();
  127 |     await cell.waitFor({ state: 'visible', timeout: 15000 });
  128 |     await cell.click();
  129 |     await page.waitForLoadState('networkidle').catch(() => { /* noop */ });
  130 |     await expect(page.getByPlaceholder('Search Patient', { exact: true }).first())
  131 |         .toBeVisible({ timeout: 15000 });
  132 | }
  133 | 
  134 | test.describe('Interaction Billing (RPM) — CPT 99457 + 99458×2 Qualification (60 min)', () => {
  135 |     let loginPage: ProviderLoginPage;
  136 | 
  137 |     test.beforeEach(async ({ page }) => {
  138 |         test.setTimeout(300000);
  139 |         loginPage = new ProviderLoginPage(page);
  140 |         Logger.step('Login to Provider Portal');
  141 |         await page.goto(providerPortalUrl, { waitUntil: 'domcontentloaded', timeout: 60000 });
  142 |         await loginPage.login(providerCredentials.username, providerCredentials.password);
  143 |     });
  144 | 
  145 |     test('care coordinator logs 60 min on RPM — patient qualifies for 99457 + 99458 (≥ 2 increments)', async ({ page }) => {
  146 |         // ============================================================
  147 |         // STEP 1 — Patient chart > RPM > Manual Time Entries (log 60 min)
  148 |         // ============================================================
  149 |         await test.step('Step 1: log a 60-min manual time entry on the chart RPM tab', async () => {
  150 |             await openPatientRpmTab(page, testPatientName);
  151 | 
  152 |             const manualBtn = page.getByRole('button', { name: /Manual Time Entries/i }).first();
  153 |             await manualBtn.waitFor({ state: 'attached', timeout: 15000 });
  154 |             await expect(
  155 |                 manualBtn,
  156 |                 `${testPatientName} must have an active RPM enrollment (Manual Time Entries should be enabled)`,
  157 |             ).toBeEnabled({ timeout: 10000 });
  158 | 
  159 |             await logManualTime(page, MINUTES_TO_LOG, 'Auto entry — CPT 99457+99458x2 qualification (60 min)');
  160 |             Logger.info(`${MINUTES_TO_LOG} min logged for ${testPatientName} on RPM tab`);
  161 | 
  162 |             await expect(
  163 |                 page.locator('//p[starts-with(normalize-space(text()),"TOTAL TIME")]').first(),
  164 |                 'Chart INTERACTIONS panel should reflect the new manual entry',
  165 |             ).toBeVisible({ timeout: 15000 });
  166 |         });
  167 | 
  168 |         // ============================================================
  169 |         // STEP 2 — Billing > Interaction > RPM (current calendar month)
  170 |         // ============================================================
  171 |         await test.step('Step 2: navigate to Billing > Interaction > RPM for the current month', async () => {
  172 |             await gotoBillingDirect(page);
  173 |             await expect(page.getByText(/^Interaction$/, { exact: true }).first())
  174 |                 .toBeVisible({ timeout: 20000 });
  175 | 
  176 |             const today = todayMmDdYyyy();
  177 |             await pickBillingMonth(page, mmDdYyyyToShortMonth(today));
  178 |             await selectBillingInteractionRpm(page);
  179 |         });
  180 | 
  181 |         // ============================================================
  182 |         // STEP 3 — Verify three CPT codes qualified (99457 + 99458×2)
  183 |         // ============================================================
  184 |         await test.step('Step 3: verify CPT CODE column lists 99457 + 99458 with ≥ 60 min total', async () => {
  185 |             const lastName = testPatientName.split(',')[0].trim();
  186 |             const searchInput = page.getByPlaceholder('Search Patient', { exact: true }).first();
  187 |             await searchInput.fill(lastName);
  188 | 
  189 |             const patientRow = page
  190 |                 .locator(`xpath=//*[normalize-space(text())="${testPatientName}"]/ancestor::*[count(*) >= 5][1]`)
  191 |                 .first();
  192 |             await expect(
  193 |                 patientRow,
  194 |                 `Patient row for ${testPatientName} should appear in Billing > Interaction > RPM ` +
  195 |                 `after logging ${MINUTES_TO_LOG} min`,
> 196 |             ).toBeVisible({ timeout: 15000 });
      |               ^ Error: Patient row for Block, Micheal should appear in Billing > Interaction > RPM after logging 60 min
  197 | 
  198 |             const rowText = (await patientRow.textContent({ timeout: 10000 })) ?? '';
  199 |             Logger.info(`Billing Interaction (RPM) row text for ${testPatientName}: ${rowText.slice(0, 250)}`);
  200 | 
  201 |             expect(rowText, 'Row should be keyed by CRITERIA = TIME')
  202 |                 .toMatch(/TIME/);
  203 | 
  204 |             const timeMatch = rowText.match(/(\d+):(\d{2})/);
  205 |             expect(
  206 |                 timeMatch,
  207 |                 'Row must include a # TIME value in MIN:SEC format',
  208 |             ).not.toBeNull();
  209 |             // Column format is MIN:SEC (e.g. "280:00" = 280 min), not HH:MM.
  210 |             const totalMinutes = timeMatch
  211 |                 ? Number(timeMatch[1]) + Number(timeMatch[2]) / 60
  212 |                 : 0;
  213 |             Logger.info(`# TIME on Billing Interaction RPM row: ${timeMatch?.[0]} (${totalMinutes} min total)`);
  214 | 
  215 |             // Three-CPT qualification floor: ≥ 60 min unlocks 99457 + 99458×2.
  216 |             expect(
  217 |                 totalMinutes,
  218 |                 `Accumulated # TIME must be ≥ ${CPT_99458x2_FLOOR_MINUTES} min ` +
  219 |                 `(third-CPT floor: 99457 + 99458 first increment + 99458 second increment)`,
  220 |             ).toBeGreaterThanOrEqual(CPT_99458x2_FLOOR_MINUTES);
  221 | 
  222 |             // CPT CODE column must list both 99457 and 99458.
  223 |             expect(
  224 |                 rowText,
  225 |                 'CPT CODE column must include 99457 (first 20 min)',
  226 |             ).toMatch(/99457/);
  227 |             expect(
  228 |                 rowText,
  229 |                 'CPT CODE column must include 99458 (additional 20-min increments)',
  230 |             ).toMatch(/99458/);
  231 | 
  232 |             // Bonus check: pull the 99458 multiplier (e.g. "* 2", "* 10") and
  233 |             // verify it's at least 2 — the qualification floor for the third
  234 |             // CPT in this scenario. Pattern matches "99458 * N" or "99458*N".
  235 |             const multiplierMatch = rowText.match(/99458\s*\*\s*(\d+)/);
  236 |             if (multiplierMatch) {
  237 |                 const multiplier = Number(multiplierMatch[1]);
  238 |                 Logger.info(`99458 multiplier on row: × ${multiplier}`);
  239 |                 expect(
  240 |                     multiplier,
  241 |                     `99458 multiplier must be ≥ 2 (the qualification floor for the "three CPT codes" scenario)`,
  242 |                 ).toBeGreaterThanOrEqual(2);
  243 |             } else {
  244 |                 Logger.info('99458 has no explicit multiplier on the row — assumed × 1, but total minutes confirms ≥ 60');
  245 |             }
  246 | 
  247 |             Logger.info(
  248 |                 `CPT 99457 + 99458×2 qualification verified: ${testPatientName} appears in Billing > ` +
  249 |                 `Interaction > RPM with CRITERIA = TIME, # TIME ≥ ${CPT_99458x2_FLOOR_MINUTES} min, ` +
  250 |                 `and CPT CODE column lists 99457 AND 99458 with multiplier ≥ 2`,
  251 |             );
  252 |         });
  253 |     });
  254 | });
  255 | 
```