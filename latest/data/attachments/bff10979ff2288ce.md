# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: provider-portal/care-team-worklist/interaction-week3.spec.ts >> Interaction - Week 3 RTM >> should complete Follow Up Meal Plan activity with time entry
- Location: tests/web/provider-portal/care-team-worklist/interaction-week3.spec.ts:164:9

# Error details

```
TimeoutError: locator.waitFor: Timeout 10000ms exceeded.
Call log:
  - waiting for getByRole('tab', { name: 'RTM' }) to be visible

```

# Page snapshot

```yaml
- generic [ref=e6]:
  - generic [ref=e9]:
    - heading "Hey, good to see you" [level=3] [ref=e10]
    - heading "Lets Sign in you" [level=4] [ref=e11]
    - textbox "Enter Username" [ref=e14]: chirag123
    - generic [ref=e16]:
      - textbox "Enter Password" [ref=e17]: Pass@123
      - button "toggle password visibility" [ref=e18] [cursor=pointer]:
        - img [ref=e19]
    - button "Forgot password?" [ref=e21] [cursor=pointer]
    - button "Login" [active] [ref=e22] [cursor=pointer]: Login
  - generic [ref=e24]:
    - heading "Unity Health" [level=3] [ref=e25]
    - paragraph [ref=e26]: Connecting Healthcare Everywhere
```

# Test source

```ts
  1   | import { expect, Locator, Page } from '@playwright/test';
  2   | import { BasePage } from '../../../base.page';
  3   | import { Logger } from '../../../../utils/logger';
  4   | import { Helpers } from '../../../../utils/helpers';
  5   | 
  6   | // Activity types for each week
  7   | export type Week1Activity = 'Reach Out' | 'Update EHR Chart' | 'Challenge' | 'Bingo' | 'Exercise Program' | 'Meal Plan';
  8   | export type Week2Activity = 'Reach Out';
  9   | export type Week3Activity = 'Reach Out' | 'Follow Up Challenge' | 'Follow Up Bingo' | 'Follow Up Exercise Program' | 'Follow Up Meal Plan';
  10  | export type Week4Activity = 'Reach Out' | 'Patient Summary' | 'Won Challenge' | 'Won Bingo';
  11  | export type WeekActivity = Week1Activity | Week2Activity | Week3Activity | Week4Activity;
  12  | 
  13  | export class WeekInteractionPage extends BasePage {
  14  |     // ── Existing selectors ───────────────────────────────────────────
  15  |     private readonly rtmTab = "//button[text()='RTM']";
  16  |     private readonly selectActionDropdown = "//div[contains(@class, 'MuiFormControl-root')]//input[@placeholder='Select'] | //div[@role='combobox'][@aria-label='Select']";
  17  |     private readonly templateSearchInput = "//input[@placeholder='Search & Select Template'] | //input[@aria-label='Search & Select Template']";
  18  |     private readonly logAsDropdown = "//div[contains(@class, 'MuiFormControl-root')]//input[@placeholder='Log As'] | //div[@role='combobox'][@aria-label='Log As']";
  19  |     private readonly sendButton = "//button/h6[text()='Send']";
  20  |     private readonly cancelButton = "//button[text()='Cancel']";
  21  |     private readonly txtMsg = "//p[text()='Text Message']/../following-sibling::p";
  22  |     private readonly loggedAt = "(//input[@placeholder='Logged By'])[2]";
  23  |     private loggedAtTime!: string;
  24  |     private automatedNote: string = `Automated message sent via Playwright test.${Helpers.getRandomNumber(1000, 9999)}`;
  25  | 
  26  |     // ── Interaction table selectors (div-based layout, NOT <table>) ──
  27  |     // From accessibility tree: header row has children with text like "NAME", "CARE MANAGER", etc.
  28  |     // Data rows are sibling divs of the header row
  29  |     // Activity checkboxes all share aria-label "introduction checkbox"
  30  |     private readonly noteTextarea = "//textarea[@name='note'] | //textarea";
  31  |     private readonly toastSelector = "//div[contains(@class,'MuiAlert-message')] | //div[contains(@class,'Toastify')] | //div[@role='alert']";
  32  |     private readonly logAsInput = "//input[@placeholder='Log As']";
  33  |     private readonly saveButton = "//button[contains(@class,'MuiButton')]//h6[text()='Save'] | //button[text()='Save']";
  34  | 
  35  |     constructor(page: Page) {
  36  |         super(page);
  37  |     }
  38  | 
  39  |     /**
  40  |      * Ensures the "Care Team Work List" tab is active before interacting with
  41  |      * its sub-tabs (RPM / RTM / CCM / PCM). The portal lands on the "Provider
  42  |      * Work List" view by default.
  43  |      */
  44  |     private async ensureCareTeamWorkListActive(): Promise<void> {
  45  |         // The RPM/RTM/CCM/PCM tablist renders on both "Provider Work List" and
  46  |         // "Care Team Work List" views. The "Interaction" paragraph is unique
  47  |         // to Care Team, so check for it before clicking — a redundant click
  48  |         // on the heading resets the tab to the favorite program, which then
  49  |         // silently breaks downstream week-selection logic.
  50  |         const interactionIndicator = this.page.getByText('Interaction', { exact: true }).first();
  51  |         if (await interactionIndicator.isVisible({ timeout: 2000 }).catch(() => false)) {
  52  |             return;
  53  |         }
  54  |         const careTeamTab = this.page.getByRole('heading', { name: 'Care Team Work List', exact: true });
  55  |         if (await careTeamTab.isVisible({ timeout: 5000 }).catch(() => false)) {
  56  |             await careTeamTab.click();
  57  |             await this.page.waitForLoadState('networkidle');
  58  |             await interactionIndicator
  59  |                 .waitFor({ state: 'visible', timeout: 8000 })
  60  |                 .catch(() => { /* older pages may label the section differently */ });
  61  |         }
  62  |     }
  63  | 
  64  |     // ═══════════════════════════════════════════════════════════════════
  65  |     // EXISTING METHODS (preserved)
  66  |     // ═══════════════════════════════════════════════════════════════════
  67  | 
  68  |     /**
  69  |      * Navigates to the RTM tab.
  70  |      */
  71  |     async navigateToRTM(): Promise<void> {
  72  |         Logger.step('Navigating to RTM tab');
  73  |         try {
  74  |             // Landing page defaults to "Provider Work List" — ensure Care Team Work List
  75  |             // tab is active before looking for the RTM sub-tab.
  76  |             await this.ensureCareTeamWorkListActive();
  77  | 
  78  |             // Click the RTM tab. Each program tab has an inner star-favorite
  79  |             // button, and a pointer click sometimes lands on that button
  80  |             // instead of selecting the tab. Try a pointer click first, then
  81  |             // fall back to a JS-dispatched click, then keyboard navigation
  82  |             // from the RPM tab (MUI Tabs respond to ArrowRight + Enter).
  83  |             const rtmTabByRole = this.page.getByRole('tab', { name: 'RTM' });
  84  |             const rpmTabByRole = this.page.getByRole('tab', { name: 'RPM' });
> 85  |             await rtmTabByRole.waitFor({ state: 'visible', timeout: 10000 });
      |                                ^ TimeoutError: locator.waitFor: Timeout 10000ms exceeded.
  86  | 
  87  |             const ensureRtmSelected = async (): Promise<boolean> => {
  88  |                 return (await rtmTabByRole.getAttribute('aria-selected')) === 'true';
  89  |             };
  90  | 
  91  |             await rtmTabByRole.click();
  92  |             await this.page.waitForLoadState('networkidle');
  93  | 
  94  |             if (!(await ensureRtmSelected())) {
  95  |                 await rtmTabByRole.evaluate((el) => (el as HTMLElement).click());
  96  |                 await this.page.waitForLoadState('networkidle');
  97  |                 await this.page.waitForTimeout(400);
  98  |             }
  99  | 
  100 |             if (!(await ensureRtmSelected())) {
  101 |                 // Keyboard fallback: focus RPM tab, then arrow-right to RTM, then Enter.
  102 |                 await rpmTabByRole.focus();
  103 |                 await this.page.keyboard.press('ArrowRight');
  104 |                 await this.page.keyboard.press('Enter');
  105 |                 await this.page.waitForLoadState('networkidle');
  106 |                 await this.page.waitForTimeout(400);
  107 |             }
  108 | 
  109 |             if (!(await ensureRtmSelected())) {
  110 |                 throw new Error('Failed to select RTM tab after pointer, JS-click, and keyboard fallbacks');
  111 |             }
  112 |             Logger.info('Successfully navigated to RTM tab');
  113 |         } catch (error) {
  114 |             Logger.error('Failed to navigate to RTM tab', error as Error);
  115 |             throw error;
  116 |         }
  117 |     }
  118 | 
  119 |     /**
  120 |      * Selects a specific week tab.
  121 |      * @param weekNumber - The week number to select (e.g., 1, 2, 3, 4).
  122 |      */
  123 |     async selectWeek(weekNumber: number): Promise<void> {
  124 |         Logger.step(`Selecting Week ${weekNumber}`);
  125 |         try {
  126 |             // Scope to the "Interaction" summary table — Week cells with the
  127 |             // same label also appear in other sections (e.g. the week tab bar
  128 |             // of a detail view), and `.first()` over the unscoped document
  129 |             // can pick the wrong one and navigate to the wrong program.
  130 |             const weekTab = this.page
  131 |                 .locator(`xpath=//*[normalize-space(text())="Interaction"]/following::table[1]//*[normalize-space(text())="Week ${weekNumber}"]`)
  132 |                 .first();
  133 |             const fallback = this.page.getByText(`Week ${weekNumber}`, { exact: true }).first();
  134 |             const target = (await weekTab.count()) > 0 ? weekTab : fallback;
  135 |             await target.waitFor({ state: 'visible', timeout: 10000 });
  136 |             await target.click();
  137 |             await this.page.waitForLoadState('networkidle');
  138 |             await this.page.waitForTimeout(800);
  139 |             Logger.info(`Successfully selected Week ${weekNumber}`);
  140 |         } catch (error) {
  141 |             Logger.error(`Failed to select Week ${weekNumber}`, error as Error);
  142 |             throw error;
  143 |         }
  144 |     }
  145 | 
  146 |     /**
  147 |      * Selects a patient by name using the introduction checkbox.
  148 |      * @param patientName - Name of the patient to select.
  149 |      */
  150 |     async selectPatient(patientName: string): Promise<void> {
  151 |         Logger.step(`Selecting patient: ${patientName}`);
  152 |         try {
  153 |             const row = this.patientRow(patientName);
  154 |             await row.waitFor({ state: 'visible', timeout: 10000 });
  155 | 
  156 |             // Row select checkbox is the first child's checkbox
  157 |             const rowCheckbox = row.locator('input[type="checkbox"]').first();
  158 |             if (await rowCheckbox.isVisible({ timeout: 3000 })) {
  159 |                 await rowCheckbox.check();
  160 |             } else {
  161 |                 // Fallback to global first introduction checkbox
  162 |                 await this.page.locator("(//input[@aria-label='introduction checkbox'])[1]").click();
  163 |             }
  164 | 
  165 |             Logger.info(`Successfully selected patient: ${patientName}`);
  166 |         } catch (error) {
  167 |             Logger.error(`Failed to select patient: ${patientName}`, error as Error);
  168 |             throw error;
  169 |         }
  170 |     }
  171 | 
  172 |     /**
  173 |      * Selects the 'Text Message' option from the action dropdown.
  174 |      */
  175 |     async selectTextMessageAction(): Promise<void> {
  176 |         Logger.step('Selecting Text Message action');
  177 |         try {
  178 |             // The current build no longer exposes a "Text Message / App Chat"
  179 |             // action dropdown after selecting a patient. The bulk-action
  180 |             // workflow uses "Reach Out All" to open the SEND MESSAGE dialog
  181 |             // (Template + Note + Log As + Send) — same path the
  182 |             // interaction-bulk tests already use.
  183 |             await this.clickReachOutAll();
  184 |             Logger.info('Reach Out All clicked — message dialog opened');
  185 |         } catch (error) {
```