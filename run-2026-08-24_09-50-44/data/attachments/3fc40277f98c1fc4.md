# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: provider-portal/enrollment-billing/rpm-billing-workflow.spec.ts >> Enrollment Billing — RPM Workflow >> 2. End-to-end report — single patient PDF
- Location: tests/web/provider-portal/enrollment-billing/rpm-billing-workflow.spec.ts:252:9

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('//*[normalize-space(text())="Reilly, Sheryl"]/ancestor::*[count(*) >= 5][1]').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('//*[normalize-space(text())="Reilly, Sheryl"]/ancestor::*[count(*) >= 5][1]').first()

```

```yaml
- banner:
  - img "Logo"
  - combobox "Search Patient Name, DOB (MM-DD-YYYY), Phone"
  - button "Open"
  - button "New Patient":
    - paragraph: New Patient
  - button
  - button
- list:
  - listitem: Virtual Services
  - list:
    - listitem: Care Team Work List
    - listitem: Enrollments
    - listitem: Patient Assessments
    - listitem: Alerts
    - separator
    - listitem: Tasks 164
    - listitem: Communications
    - separator
    - listitem: Billing
    - separator
    - listitem: Reports
    - separator
    - listitem: Document Library
    - listitem: CCM Library
    - listitem: PCM Library
    - listitem: RPM Library
    - listitem: RTM Library
    - separator
    - listitem: Training Library
  - listitem: Call Center
  - listitem: Revenue Cycle Management
  - listitem: Omni Channel
  - separator
  - listitem: Settings
- paragraph: Jemes Cory
- list:
  - listitem: Logout
- listitem
- main:
  - heading "Billing" [level=6]
  - button "Choose date, selected date is Aug 1, 2026"
  - textbox "Select Month": Aug 2026
  - text: Enrollments
  - table "simple table":
    - rowgroup:
      - row "RPM RTM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
    - rowgroup:
      - row "To Be Billed 115 639":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "115":
          - paragraph: "115"
        - cell "639":
          - paragraph: "639"
      - row "Submitted 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - text: Monitoring
  - table "simple table":
    - rowgroup:
      - row "RPM RTM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
    - rowgroup:
      - row "To Be Billed 0 0":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
      - row "Submitted 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - text: Interaction
  - table "simple table":
    - rowgroup:
      - row "RPM RTM CCM PCM":
        - columnheader:
          - paragraph
        - columnheader "RPM":
          - paragraph: RPM
        - columnheader "RTM":
          - paragraph: RTM
        - columnheader "CCM":
          - paragraph: CCM
        - columnheader "PCM":
          - paragraph: PCM
    - rowgroup:
      - row "To Be Billed 0 16 0 0":
        - cell "To Be Billed":
          - paragraph: To Be Billed
        - cell "0":
          - paragraph: "0"
        - cell "16":
          - paragraph: "16"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
      - row "Submitted 0 0 0 0":
        - cell "Submitted":
          - paragraph: Submitted
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
        - cell "0":
          - paragraph: "0"
  - heading "INTERACTION (RPM)" [level=5]
  - textbox "Search Patient": Reilly
  - textbox "Search By Insurance"
  - checkbox "SelfPay"
  - text: SelfPay
  - combobox "Select": All Providers
  - button "Open"
  - combobox: Claim Status
  - combobox: Paid Status
  - button "Export Billing"
  - checkbox "select all rows"
  - text: "NAME ▲ SERVICE CRITERIA # TIME DIAGNOSIS CODES(ICD) PROVIDER CPT CODE INSURANCE CLAIM NUMBER CLAIM STATUS PAID DATE PAID STATUS ACTION"
  - heading "No data found." [level=6]
- button
- button
```

# Test source

```ts
  156 | }
  157 | 
  158 | // ====================================================================
  159 | // SUITE
  160 | // ====================================================================
  161 | 
  162 | test.describe('Enrollment Billing — RPM Workflow', () => {
  163 |     // All 11 tests share the same patient's billing state. Run serial
  164 |     // within the describe so simultaneous workers don't corrupt each
  165 |     // other's manual time entries and billing reads.
  166 |     test.describe.configure({ mode: 'serial' });
  167 |     let loginPage: ProviderLoginPage;
  168 |     let searchPage: GlobalSearchPage;
  169 |     let PATIENT_DISPLAY = '';
  170 |     let PATIENT_LAST = '';
  171 |     let PATIENT_FIRST = '';
  172 | 
  173 |     test.beforeEach(async ({ page, createEnrolledPatient }) => {
  174 |         // First run also seeds the patient (~3 min); 420s covers that plus a
  175 |         // slow CI runner. Non-seeding runs finish well inside the budget.
  176 |         test.setTimeout(420000);
  177 |         loginPage = new ProviderLoginPage(page);
  178 |         searchPage = new GlobalSearchPage(page);
  179 | 
  180 |         Logger.step('Login to Provider Portal');
  181 |         await page.goto(providerPortalUrl);
  182 |         await loginPage.login(providerCredentials.username, providerCredentials.password);
  183 | 
  184 |         // Seed our own RPM patient ONCE per file (guarded — serial tests reuse it).
  185 |         if (!PATIENT_DISPLAY) {
  186 |             const patient = await createEnrolledPatient({ state: 'Active', program: 'RPM' });
  187 |             PATIENT_DISPLAY = patient.displayName;
  188 |             PATIENT_LAST = patient.lastName;
  189 |             PATIENT_FIRST = patient.firstName;
  190 |         }
  191 | 
  192 |         // Open the patient chart.
  193 |         await searchPage.searchFor(PATIENT_FIRST);
  194 |         const found = await searchPage.isPatientInDropdown(PATIENT_DISPLAY);
  195 |         expect(found, `Patient "${PATIENT_DISPLAY}" should be in global-search dropdown`).toBe(true);
  196 |         await searchPage.selectPatientFromDropdown(PATIENT_DISPLAY);
  197 |         // The chart no longer renders the patient name in a heading tag — the
  198 |         // build moved it out of the old <h5> into a plain element. Match the
  199 |         // display-name text wherever it appears in the chart header rather than
  200 |         // requiring a specific heading tag.
  201 |         await expect(
  202 |             page.getByText(PATIENT_DISPLAY, { exact: true }).first(),
  203 |         ).toBeVisible({ timeout: 15000 });
  204 | 
  205 |         // Touch RTM then RPM program tabs (one-time chart wiring step).
  206 |         for (const program of ['RTM', 'RPM'] as const) {
  207 |             const btn = page.getByRole('button', { name: new RegExp(`^${program}$`, 'i') }).first();
  208 |             if (await btn.isEnabled({ timeout: 8000 }).catch(() => false)) {
  209 |                 await btn.click();
  210 |                 await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  211 |             }
  212 |         }
  213 | 
  214 |         // Log time so the patient appears in the Interaction bucket.
  215 |         await logManualTime(page, 10, 'Automated billing flow — manual time setup');
  216 |         await expect(
  217 |             page.locator('//p[starts-with(normalize-space(text()),"TOTAL TIME")]').first(),
  218 |         ).toBeVisible({ timeout: 15000 });
  219 | 
  220 |         // Navigate to Billing > Interaction > RPM.
  221 |         await openSidebarItem(page, 'Billing');
  222 |         await expect(page.getByText(/^Interaction$/, { exact: true }).first())
  223 |             .toBeVisible({ timeout: 20000 });
  224 |         await selectInteractionProgram(page, 'RPM');
  225 |     });
  226 | 
  227 |     // ────────────────────────────────────────────────────────────────
  228 |     // SCENARIO 1: Bucket counts render
  229 |     // ────────────────────────────────────────────────────────────────
  230 |     test('1. Billing page bucket counts display for RPM', async ({ page }) => {
  231 |         const panel = sectionPanel(page, 'Interaction');
  232 |         await expect(panel.getByText('To Be Billed').first()).toBeVisible({ timeout: 10000 });
  233 |         await expect(panel.getByText('Submitted').first()).toBeVisible({ timeout: 10000 });
  234 | 
  235 |         // Strict: the RPM cell of each row must hold a numeric value.
  236 |         // Number.isFinite alone was tautological — `parseInt || 0`
  237 |         // always coerces to a finite 0.
  238 |         const tBilledCell = panel.locator(`xpath=.//tr[.//*[normalize-space()="To Be Billed"]]/td[2]`).first();
  239 |         const submittedCell = panel.locator(`xpath=.//tr[.//*[normalize-space()="Submitted"]]/td[2]`).first();
  240 |         await expect(tBilledCell).toHaveText(/^\s*\d+\s*$/, { timeout: 10000 });
  241 |         await expect(submittedCell).toHaveText(/^\s*\d+\s*$/, { timeout: 10000 });
  242 | 
  243 |         Logger.info(
  244 |             `Interaction RPM buckets — To Be Billed: ${await readInteractionBucket(page, 'RPM', 'To Be Billed')}, ` +
  245 |             `Submitted: ${await readInteractionBucket(page, 'RPM', 'Submitted')}`,
  246 |         );
  247 |     });
  248 | 
  249 |     // ────────────────────────────────────────────────────────────────
  250 |     // SCENARIO 2: Single-patient PDF export
  251 |     // ────────────────────────────────────────────────────────────────
  252 |     test('2. End-to-end report — single patient PDF', async ({ page }) => {
  253 |         const searchPatient = page.getByPlaceholder('Search Patient', { exact: true }).first();
  254 |         await searchPatient.waitFor({ state: 'visible', timeout: 10000 });
  255 |         await searchPatient.fill(PATIENT_LAST);
> 256 |         await expect(billingRow(page, PATIENT_DISPLAY)).toBeVisible({ timeout: 15000 });
      |                                                         ^ Error: expect(locator).toBeVisible() failed
  257 | 
  258 |         const rowCheckboxWrapper = billingRow(page, PATIENT_DISPLAY)
  259 |             .locator('xpath=.//input[@type="checkbox"]/ancestor::*[1]')
  260 |             .first();
  261 |         await rowCheckboxWrapper.click();
  262 |         // Hard-assert the checkbox flipped — silently catching this
  263 |         // turned the assertion into a no-op.
  264 |         await expect(billingRow(page, PATIENT_DISPLAY).locator('input[type="checkbox"]'))
  265 |             .toBeChecked({ timeout: 5000 });
  266 | 
  267 |         const exportBtn = page.getByRole('button', { name: /Export Billing/i }).first();
  268 |         const downloadPromise = page.waitForEvent('download', { timeout: 30000 });
  269 |         await exportBtn.click();
  270 | 
  271 |         const pdfOption = page.getByRole('menuitem', { name: /PDF/i })
  272 |             .or(page.getByRole('option', { name: /PDF/i }))
  273 |             .or(page.getByRole('radio', { name: /PDF/i }));
  274 |         if (await pdfOption.first().isVisible({ timeout: 5000 }).catch(() => false)) {
  275 |             await pdfOption.first().click();
  276 |             const confirm = page.getByRole('button', { name: /Download|Generate|Confirm|Export/i }).last();
  277 |             if (await confirm.isVisible({ timeout: 3000 }).catch(() => false)) {
  278 |                 await confirm.click();
  279 |             }
  280 |         }
  281 | 
  282 |         const download = await downloadPromise;
  283 |         expect(download, 'Export Billing should produce a download').not.toBeNull();
  284 |         expect(download.suggestedFilename().toLowerCase()).toMatch(/\.(pdf|csv|xlsx?)$/);
  285 |     });
  286 | 
  287 |     // ────────────────────────────────────────────────────────────────
  288 |     // SCENARIO 3: Multi-patient CSV export
  289 |     // ────────────────────────────────────────────────────────────────
  290 |     test('3. End-to-end report — multiple patients CSV', async ({ page }) => {
  291 |         const exportBtn = page.getByRole('button', { name: /Export Billing/i }).first();
  292 |         await exportBtn.waitFor({ state: 'visible', timeout: 10000 });
  293 | 
  294 |         const downloadPromise = page.waitForEvent('download', { timeout: 30000 });
  295 |         await exportBtn.click();
  296 | 
  297 |         const csvOption = page.getByRole('menuitem', { name: /CSV/i })
  298 |             .or(page.getByRole('option', { name: /CSV/i }))
  299 |             .or(page.getByRole('radio', { name: /CSV/i }));
  300 |         if (await csvOption.first().isVisible({ timeout: 5000 }).catch(() => false)) {
  301 |             await csvOption.first().click();
  302 |             const confirm = page.getByRole('button', { name: /Download|Generate|Confirm|Export/i }).last();
  303 |             if (await confirm.isVisible({ timeout: 3000 }).catch(() => false)) {
  304 |                 await confirm.click();
  305 |             }
  306 |         }
  307 | 
  308 |         const download = await downloadPromise;
  309 |         expect(download, 'Export Billing should produce a download').not.toBeNull();
  310 |         expect(download.suggestedFilename().toLowerCase()).toMatch(/\.(csv|xlsx?)$/);
  311 |     });
  312 | 
  313 |     // ────────────────────────────────────────────────────────────────
  314 |     // SCENARIO 4: Patient + Insurance + Claim Status filter combo
  315 |     // ────────────────────────────────────────────────────────────────
  316 |     test('4. Patient search + multi-filter (Insurance, Claim Status)', async ({ page }) => {
  317 |         const searchPatient = page.getByPlaceholder('Search Patient', { exact: true }).first();
  318 |         await searchPatient.fill(PATIENT_LAST);
  319 | 
  320 |         // Establish the patient row baseline BEFORE applying further
  321 |         // filters. The test verifies the filter mechanism, not a
  322 |         // specific insurance string — fail fast if the patient isn't
  323 |         // even findable by name (that's a different bug).
  324 |         await expect(billingRow(page, PATIENT_DISPLAY))
  325 |             .toBeVisible({ timeout: 15000 });
  326 | 
  327 |         const searchInsurance = page.getByPlaceholder('Search By Insurance').first();
  328 |         await searchInsurance.fill('SelfPay');
  329 |         await applyDropdownByLabel(page, /Claim Status/i, /All|N\/A/i);
  330 | 
  331 |         // Real assertion: the filter mechanism re-renders the table.
  332 |         // Either the patient row is still visible (their insurance is
  333 |         // SelfPay) OR a "No records" empty state appears (insurance is
  334 |         // not SelfPay — also a valid filter outcome). Anchoring on
  335 |         // exactly one of these states confirms the filter wired up —
  336 |         // the previous version checked nothing meaningful.
  337 |         const row = billingRow(page, PATIENT_DISPLAY);
  338 |         const emptyState = page.getByText(/No data found|No Records Found|No records to display/i).first();
  339 |         await expect(async () => {
  340 |             const rowVisible = await row.isVisible({ timeout: 1000 }).catch(() => false);
  341 |             const emptyVisible = await emptyState.isVisible({ timeout: 1000 }).catch(() => false);
  342 |             expect(rowVisible || emptyVisible,
  343 |                 'Filter must produce either the row or an explicit empty state',
  344 |             ).toBeTruthy();
  345 |         }).toPass({ timeout: 15000 });
  346 |     });
  347 | 
  348 |     // ────────────────────────────────────────────────────────────────
  349 |     // SCENARIO 5: Provider filter updates buckets dynamically
  350 |     // ────────────────────────────────────────────────────────────────
  351 |     test('5. Provider filter updates bucket counts dynamically', async ({ page }) => {
  352 |         const before = await readInteractionBucket(page, 'RPM', 'To Be Billed');
  353 | 
  354 |         // Pick whatever the FIRST real provider option is — don't
  355 |         // hardcode a name (the dropdown contents depend on the test
  356 |         // env's seed providers). Read the option's name dynamically so
```