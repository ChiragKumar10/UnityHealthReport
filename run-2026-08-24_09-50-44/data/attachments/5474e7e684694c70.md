# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: provider-portal/patient-wallet/patient-medical-history-workflow.spec.ts >> Patient Wallet - Medical History Full Workflow >> AUT_PT_MH_001 - Complete Medical History workflow in patient chart (11 scenarios)
- Location: tests/web/provider-portal/patient-wallet/patient-medical-history-workflow.spec.ts:18:9

# Error details

```
Error: Escalations list must include PROVIDER column

expect(locator).toContainText(expected) failed

Locator: locator('main')
Expected pattern: /PROVIDER/i
Received string:  "RPMRTMCCMPCMSkilesWeimann, ChloeMale | 06-02-2023 |CCM - Enrolled 08-24-2026User hasn't logged inProfileUpcoming Appointment-Last Appointment-Global CommentNewType Your Comment...Text MessageChatsCare PlanAssessmentTasksDocumentsHome ExerciseMedical HistoryEnrollmentsCare TeamTraining LibraryBillingSummaryDiagnosesEscalationsMedicationAllergiesAppointment Alerts​​+ Add Appointment AlertNOAPPOINTMENT NAMEAPPOINTMENT DATEAPPOINTMENT TIMEFACILITYNOTESTATUSRESOLVED NOTENo rows"
Timeout: 10000ms

Call log:
  - Escalations list must include PROVIDER column with timeout 10000ms
  - waiting for locator('main')
    23 × locator resolved to <main class="MuiBox-root css-5hqkkr">…</main>
       - unexpected value "RPMRTMCCMPCMSkilesWeimann, ChloeMale | 06-02-2023 |CCM - Enrolled 08-24-2026User hasn't logged inProfileUpcoming Appointment-Last Appointment-Global CommentNewType Your Comment...Text MessageChatsCare PlanAssessmentTasksDocumentsHome ExerciseMedical HistoryEnrollmentsCare TeamTraining LibraryBillingSummaryDiagnosesEscalationsMedicationAllergiesAppointment Alerts​​+ Add Appointment AlertNOAPPOINTMENT NAMEAPPOINTMENT DATEAPPOINTMENT TIMEFACILITYNOTESTATUSRESOLVED NOTENo rows"

```

```yaml
- main:
  - group "Platform":
    - button "RPM" [disabled]
    - button "RTM" [disabled]
    - button "CCM" [pressed]
    - button "PCM" [disabled]
  - text: SkilesWeimann, Chloe Male | 06-02-2023 | CCM - Enrolled 08-24-2026 User hasn't logged in
  - button "Profile"
  - paragraph: Upcoming Appointment
  - paragraph: "-"
  - paragraph: Last Appointment
  - paragraph: "-"
  - paragraph: Global Comment
  - button "New"
  - paragraph: Type Your Comment...
  - button "Text Message"
  - tablist:
    - tab "Chats"
    - tab "Care Plan"
    - tab "Assessment"
    - tab "Tasks"
    - tab "Documents"
    - tab "Home Exercise"
    - tab "Medical History" [selected]
    - tab "Enrollments"
    - tab "Care Team"
    - tab "Training Library"
    - tab "Billing"
    - tab "Summary"
  - group "Platform":
    - button "Diagnoses"
    - button "Escalations"
    - button "Medication"
    - button "Allergies"
    - button "Appointment Alerts" [pressed]
  - textbox "Type here to search"
  - button "+ Add Appointment Alert"
  - grid:
    - row "NO APPOINTMENT NAME APPOINTMENT DATE APPOINTMENT TIME FACILITY NOTE STATUS RESOLVED NOTE":
      - columnheader "NO"
      - columnheader "APPOINTMENT NAME"
      - columnheader "APPOINTMENT DATE"
      - columnheader "APPOINTMENT TIME"
      - columnheader "FACILITY"
      - columnheader "NOTE"
      - columnheader "STATUS"
      - columnheader "RESOLVED NOTE"
    - text: No rows
    - rowgroup
```

# Test source

```ts
  382 |             const persistedNote = await noteField.inputValue().catch(() => '');
  383 |             Logger.info(`Scenario 4: Edit-form note value = "${persistedNote}"`);
  384 |             expect(persistedNote.length, 'Edit form Note must be pre-populated (non-empty)').toBeGreaterThan(0);
  385 |             expect(persistedNote, `Edit form Note must retain the "PW-MH test" prefix`)
  386 |                 .toMatch(/PW-MH test/i);
  387 |             Logger.info('Scenario 4 ✓: Edit form pre-populated with existing data');
  388 |         });
  389 | 
  390 |         // =====================================================================
  391 |         // SCENARIO 3 + 6 — Edit workflow + Updated At/By audit
  392 |         // =====================================================================
  393 |         await test.step('Scenarios 3 + 6: Edit → save → list reflects update and shows Updated fields', async () => {
  394 |             if (!conditionRowActionAvailable) { Logger.info('Scenarios 3+6 SKIPPED — condition edit action removed from the read-only grid'); return; }
  395 |             // We're inside the Edit form from Scenario 4 — change the note then save.
  396 |             const noteInput = page.getByPlaceholder(/Enter Note|Note/i).first();
  397 |             await noteInput.waitFor({ state: 'visible', timeout: 5000 });
  398 |             await noteInput.fill('');
  399 |             await noteInput.fill(updatedNote);
  400 | 
  401 |             const saveBtn = page.getByRole('button', { name: /^Save$/ }).last();
  402 |             await saveBtn.click();
  403 |             await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  404 |             await settle(page, 2500);
  405 | 
  406 |             const body = await page.locator('main').innerText();
  407 |             // Scenario 3: the new note must appear in the list (or be persisted in the row).
  408 |             expect(body, 'Updated condition data should be reflected in the list')
  409 |                 .toMatch(new RegExp(conditionICD, 'i'));
  410 | 
  411 |             // Scenario 6: Updated-at date (today) + Updated-by (provider name) surfaces somewhere.
  412 |             const today = new Date();
  413 |             const mm = String(today.getMonth() + 1).padStart(2, '0');
  414 |             const dd = String(today.getDate()).padStart(2, '0');
  415 |             const yyyy = today.getFullYear();
  416 |             const datePatterns = [
  417 |                 `${mm}/${dd}/${yyyy}`,
  418 |                 `${mm}-${dd}-${yyyy}`,
  419 |                 `${yyyy}-${mm}-${dd}`,
  420 |             ];
  421 |             const hasDate = datePatterns.some(p => body.includes(p));
  422 |             if (hasDate) {
  423 |                 Logger.info(`Scenario 6 ✓: Updated-at date (${mm}/${dd}/${yyyy}) visible in list`);
  424 |             } else {
  425 |                 Logger.info('Scenario 6 note: updated-at date format differs — check manually');
  426 |             }
  427 | 
  428 |             // Updated-by provider name.
  429 |             const loggedInName = 'chirag kumar';
  430 |             if (body.toLowerCase().includes(loggedInName)) {
  431 |                 Logger.info(`Scenario 6 ✓: Updated-by provider "${loggedInName}" visible in list`);
  432 |             } else {
  433 |                 Logger.info('Scenario 6 note: updated-by provider name not surfaced in the list view');
  434 |             }
  435 |             Logger.info('Scenario 3 ✓: Edit saved and condition persists in the list');
  436 |         });
  437 | 
  438 |         // =====================================================================
  439 |         // SCENARIO 5 — Delete condition workflow
  440 |         // =====================================================================
  441 |         await test.step('Scenario 5: Delete condition + confirmation → removed from list', async () => {
  442 |             if (!conditionRowActionAvailable) { Logger.info('Scenario 5 SKIPPED — condition delete action removed from the read-only grid'); return; }
  443 |             await openRowActionMenu(conditionICD).catch(async () => {
  444 |                 const firstKebab = page.locator("[data-testid='MoreVertIcon']").first();
  445 |                 await firstKebab.click();
  446 |                 await settle(page, 1000);
  447 |             });
  448 |             await clickMenuItem(/^Delete$/);
  449 |             await settle(page, 2000);
  450 | 
  451 |             // Confirmation dialog.
  452 |             const confirmDialog = page.locator("[role='dialog'], .MuiDialog-root").filter({
  453 |                 has: page.getByRole('button', { name: /^Delete|^Yes|^Confirm$/i }),
  454 |             }).first();
  455 |             if (await confirmDialog.isVisible({ timeout: 5000 }).catch(() => false)) {
  456 |                 await confirmDialog.getByRole('button', { name: /^Delete|^Yes|^Confirm$/i }).first().click();
  457 |                 await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  458 |                 await settle(page, 2500);
  459 |             }
  460 | 
  461 |             // After delete, the list should not contain any row with our specific note text.
  462 |             const body = await page.locator('main').innerText();
  463 |             expect(body, 'Deleted condition note must no longer appear in the list')
  464 |                 .not.toContain(updatedNote);
  465 |             Logger.info('Scenario 5 ✓: condition deleted and removed from list');
  466 |         });
  467 | 
  468 |         // =====================================================================
  469 |         // ESCALATIONS VIEW — scenarios 7-11
  470 |         // =====================================================================
  471 |         await openMHView('Escalations');
  472 |         await settle(page, 2000);
  473 | 
  474 |         // =====================================================================
  475 |         // SCENARIO 7 — Escalations list with Provider Name / Reason / Date
  476 |         // =====================================================================
  477 |         await test.step('Scenario 7: Escalations list exposes Provider Name / Reason Escalated / Date', async () => {
  478 |             // Wait for the Escalations grid headers to render before assertions.
  479 |             const main = page.locator('main');
  480 |             await expect(main,
  481 |                 'Escalations list must include PROVIDER column')
> 482 |                 .toContainText(/PROVIDER/i, { timeout: 10000 });
      |                  ^ Error: Escalations list must include PROVIDER column
  483 |             await expect(main,
  484 |                 'Escalations list must include REASON column')
  485 |                 .toContainText(/REASON/i, { timeout: 5000 });
  486 |             await expect(main,
  487 |                 'Escalations list must include DATE column')
  488 |                 .toContainText(/DATE/i, { timeout: 5000 });
  489 |             Logger.info('Scenario 7 ✓: all required escalation columns present');
  490 |         });
  491 | 
  492 |         // =====================================================================
  493 |         // SCENARIO 11 — Search escalations by provider name
  494 |         // =====================================================================
  495 |         await test.step('Scenario 11: Search filters escalations by provider name', async () => {
  496 |             // The escalations panel uses placeholder "Type here to search" — match
  497 |             // exactly so we don't accidentally hit the global "Search Patient" combobox.
  498 |             const searchBox = page.getByRole('textbox', { name: /Type here to search/i }).first();
  499 |             if (!(await searchBox.isVisible({ timeout: 3000 }).catch(() => false))) {
  500 |                 Logger.info('Scenario 11 note: no escalations search input — skipping');
  501 |                 return;
  502 |             }
  503 |             // Use a provider name that's actually in the list (snapshot shows "Anny vate").
  504 |             const escalationProvider = 'Anny';
  505 |             // Capture pre-search table body so we can detect filter behaviour.
  506 |             const tableSel = "[role='grid'], table, .MuiDataGrid-root";
  507 |             const beforeText = await page.locator(tableSel).first().innerText().catch(() => '');
  508 |             await searchBox.fill(escalationProvider);
  509 |             await settle(page, 2500);
  510 |             const afterText = await page.locator(tableSel).first().innerText().catch(() => '');
  511 |             // Accept any of: matching provider visible, list became empty/no-data message,
  512 |             // or the visible grid content changed after typing — all are valid filter signals.
  513 |             const matched = new RegExp(escalationProvider, 'i').test(afterText);
  514 |             // Broader empty-state patterns — vendors use "No Data", "No Results",
  515 |             // "No Escalations Found", "Nothing to display", or just collapse the
  516 |             // grid body to a near-empty container.
  517 |             const emptyState = /no\s*(data|records?|result|escalat|content|items?|found)/i.test(afterText)
  518 |                 || /nothing\s*to\s*(display|show)/i.test(afterText)
  519 |                 || afterText.replace(/\s+/g, '').length < 80;
  520 |             const changed = afterText.trim() !== beforeText.trim();
  521 |             const filterWorked = matched || emptyState || changed;
  522 |             expect(filterWorked,
  523 |                 `Search "${escalationProvider}" must affect the escalations list (match, empty state, or change)`)
  524 |                 .toBeTruthy();
  525 |             await searchBox.fill('');
  526 |             await settle(page, 1500);
  527 |             Logger.info(`Scenario 11 ✓: search filters escalations list (matched=${matched}, empty=${emptyState}, changed=${changed})`);
  528 |         });
  529 | 
  530 |         // =====================================================================
  531 |         // SCENARIO 10 — Show Closed checkbox surfaces resolved escalations
  532 |         // =====================================================================
  533 |         await test.step('Scenario 10: Show Closed toggle reveals resolved escalations', async () => {
  534 |             const showClosed = page.getByRole('checkbox', { name: /Show Closed/i }).first();
  535 |             if (!(await showClosed.isVisible({ timeout: 3000 }).catch(() => false))) {
  536 |                 Logger.info('Scenario 10 note: no "Show Closed" checkbox on Escalations view — skipping');
  537 |                 return;
  538 |             }
  539 |             const beforeText = await page.locator('main').innerText();
  540 |             await showClosed.click({ force: true });
  541 |             await settle(page, 2000);
  542 |             const afterText = await page.locator('main').innerText();
  543 |             // With any closed-scoped escalations present, the text content should change.
  544 |             expect(afterText.length, 'Toggling Show Closed must re-render list content').toBeGreaterThan(0);
  545 |             Logger.info(`Scenario 10 ✓: Show Closed toggled (text delta: ${Math.abs(afterText.length - beforeText.length)} chars)`);
  546 |             // Reset toggle.
  547 |             await showClosed.click({ force: true }).catch(() => {});
  548 |             await settle(page, 1500);
  549 |         });
  550 | 
  551 |         // =====================================================================
  552 |         // SCENARIOS 8 + 9 — Resolve escalation + manual time entry
  553 |         // =====================================================================
  554 |         await test.step('Scenarios 8 + 9: Resolve escalation opens time-entry form, log time, save', async () => {
  555 |             await dismissOverlays();
  556 | 
  557 |             // Find any active escalation row's ACTION kebab (scoped to the grid so
  558 |             // we don't pick up kebabs elsewhere on the chart page).
  559 |             const gridKebab = page.locator("[role='grid'] [data-testid='MoreVertIcon']").first();
  560 |             if (!(await gridKebab.isVisible({ timeout: 5000 }).catch(() => false))) {
  561 |                 Logger.info('Scenarios 8/9 note: no escalation rows with action menu — skipping');
  562 |                 return;
  563 |             }
  564 |             await gridKebab.click();
  565 |             await settle(page, 1200);
  566 | 
  567 |             // Match Resolve in either <button> or <li role=menuitem> form.
  568 |             const resolveMenu = page.locator(
  569 |                 "[role='menu'] :text-matches('^Resolve', 'i'), [role='menuitem']:has-text('Resolve'), button:has-text('Resolve')"
  570 |             ).first();
  571 |             if (!(await resolveMenu.isVisible({ timeout: 3000 }).catch(() => false))) {
  572 |                 Logger.info('Scenarios 8/9 note: no Resolve action in kebab menu — skipping');
  573 |                 await page.keyboard.press('Escape').catch(() => {});
  574 |                 return;
  575 |             }
  576 |             await resolveMenu.click();
  577 |             await settle(page, 3000);
  578 | 
  579 |             // Scenario 9: a time-entry dialog / drawer should appear. Accept any
  580 |             // of the common heading variants the app uses across modules.
  581 |             const timeLogHeading = page.getByText(
  582 |                 /TIME\s*LOG|Time\s*Log|MANUAL\s*TIME|Manual\s*Time|Time\s*Spent|Log\s*Time/i
```