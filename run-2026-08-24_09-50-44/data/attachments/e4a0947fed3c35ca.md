# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: provider-portal/documents/documents-full-workflow.spec.ts >> Document - Unarchive and Assign >> AUT_DOC_022 - Unarchive document, assign to patient, verify in patient charting
- Location: tests/web/provider-portal/documents/documents-full-workflow.spec.ts:1414:9

# Error details

```
Error: Unarchived document "UnarcAssign 1787561240431" should appear in patient charting

expect(locator).toContainText(expected) failed

Locator: locator('main')
Expected substring: "UnarcAssign 1787561240431"
Received string:    "RPMRTMCCMPCMSpencer, TannerMale | 07-05-2024 |CCM - Enrolled 08-24-2026User hasn't logged inProfileUpcoming Appointment-Last Appointment-Global CommentNewType Your Comment...Text MessageChatsCare PlanAssessmentTasksDocumentsHome ExerciseMedical HistoryEnrollmentsCare TeamTraining LibraryBillingSummaryShow Archived​​Assign DocumentAdd DocumentHome›EducationNOFILE NAMETAGSHAREDTYPEMODIFIEDMODIFIED BYACTIONNo data found."
Timeout: 15000ms

Call log:
  - Unarchived document "UnarcAssign 1787561240431" should appear in patient charting with timeout 15000ms
  - waiting for locator('main')
    3 × locator resolved to <main class="MuiBox-root css-5hqkkr">…</main>
      - unexpected value "RPMRTMCCMPCMSpencer, TannerMale | 07-05-2024 |CCM - Enrolled 08-24-2026User hasn't logged inProfileUpcoming Appointment-Last Appointment-Global CommentNewType Your Comment...Text MessageChatsCare PlanAssessmentTasksDocumentsHome ExerciseMedical HistoryEnrollmentsCare TeamTraining LibraryBillingSummaryShow Archived​​Assign DocumentAdd DocumentHome›EducationNOFILE NAMETAGSHAREDTYPEMODIFIEDMODIFIED BYACTIONLoading..."
    30 × locator resolved to <main class="MuiBox-root css-5hqkkr">…</main>
       - unexpected value "RPMRTMCCMPCMSpencer, TannerMale | 07-05-2024 |CCM - Enrolled 08-24-2026User hasn't logged inProfileUpcoming Appointment-Last Appointment-Global CommentNewType Your Comment...Text MessageChatsCare PlanAssessmentTasksDocumentsHome ExerciseMedical HistoryEnrollmentsCare TeamTraining LibraryBillingSummaryShow Archived​​Assign DocumentAdd DocumentHome›EducationNOFILE NAMETAGSHAREDTYPEMODIFIEDMODIFIED BYACTIONNo data found."

```

```yaml
- main:
  - group "Platform":
    - button "RPM" [disabled]
    - button "RTM" [disabled]
    - button "CCM" [pressed]
    - button "PCM" [disabled]
  - text: Spencer, Tanner Male | 07-05-2024 | CCM - Enrolled 08-24-2026 User hasn't logged in
  - button "Profile"
  - paragraph: Upcoming Appointment
  - paragraph: "-"
  - paragraph: Last Appointment
  - paragraph: "-"
  - paragraph: Global Comment
  - button "New"
  - paragraph: Type Your Comment...
  - button "Text Message"
  - tablist:
    - tab "Chats"
    - tab "Care Plan"
    - tab "Assessment"
    - tab "Tasks"
    - tab "Documents" [selected]
    - tab "Home Exercise"
    - tab "Medical History"
    - tab "Enrollments"
    - tab "Care Team"
    - tab "Training Library"
    - tab "Billing"
    - tab "Summary"
  - checkbox "Show Archived"
  - text: Show Archived
  - textbox "Type here to search"
  - button "Assign Document"
  - button "Add Document"
  - navigation "breadcrumb":
    - list:
      - listitem:
        - heading "Home" [level=6]
      - listitem:
        - heading "Education" [level=6]
  - text: NO FILE NAME TAG SHARED TYPE MODIFIED MODIFIED BY ACTION
  - heading "No data found." [level=6]
```

# Test source

```ts
  1403 |     let docPage: DocumentLibraryPage;
  1404 |     const { providerCredentials, providerPortalUrl } = getEnvironmentConfig();
  1405 | 
  1406 |     test.beforeEach(async ({ page }) => {
  1407 |         loginPage = new ProviderLoginPage(page);
  1408 |         docPage = new DocumentLibraryPage(page);
  1409 |         await page.goto(providerPortalUrl);
  1410 |         await loginPage.login(providerCredentials.username, providerCredentials.password);
  1411 |         await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  1412 |     });
  1413 | 
  1414 |     test('AUT_DOC_022 - Unarchive document, assign to patient, verify in patient charting', async ({ page, createEnrolledPatient }) => {
  1415 |         test.setTimeout(420000);
  1416 |         Logger.step('Test: Unarchive → Assign → Verify in patient chart');
  1417 | 
  1418 |         // Seed our own CCM patient (the shared "Automation" patient was deleted).
  1419 |         const patient = await createEnrolledPatient({ state: 'Active', program: 'CCM' });
  1420 | 
  1421 |         // Step 1: Upload and archive a document
  1422 |         await docPage.navigateToDocumentLibrary();
  1423 |         await docPage.clickFolder('Education');
  1424 | 
  1425 |         const ts = Date.now();
  1426 |         const fn = `UnarcAssign ${ts}`;
  1427 |         await docPage.uploadDocument({
  1428 |             tag: 'Education', speciality: 'Cardiologist', fileName: fn,
  1429 |             filePath: `${docsPath}/4mb.jpg`
  1430 |         });
  1431 | 
  1432 |         await docPage.navigateToDocumentLibrary();
  1433 |         await docPage.clickFolder('Education');
  1434 |         await settle(page, 2000);
  1435 |         await docPage.searchDocument(fn);
  1436 |         await docPage.archiveDocument(0);
  1437 |         Logger.info('Step 1 ✓: Document uploaded and archived');
  1438 | 
  1439 |         // Step 2: Enable Show Archived and unarchive
  1440 |         await docPage.navigateToDocumentLibrary();
  1441 |         await docPage.clickFolder('Education');
  1442 |         await settle(page, 2000);
  1443 |         await docPage.toggleShowArchived();
  1444 |         await settle(page, 2000);
  1445 |         await docPage.searchDocument(fn);
  1446 | 
  1447 |         await docPage.clickDocumentActionMenu(0);
  1448 |         const popoverText = await page.locator(".MuiPopover-root").innerText().catch(() => '');
  1449 | 
  1450 |         if (popoverText.includes('Unarchive')) {
  1451 |             await page.getByText('Unarchive', { exact: true }).click();
  1452 |             await settle(page, 2000);
  1453 | 
  1454 |             const dialog = page.locator("[role='dialog']");
  1455 |             if (await dialog.isVisible().catch(() => false)) {
  1456 |                 await page.locator("[role='dialog'] button").filter({ hasText: /Unarchive|Yes|Confirm/ }).first().click();
  1457 |                 await settle(page, 1000);
  1458 |             }
  1459 |             await page.locator("[role='alert'], .MuiSnackbar-root").first().waitFor({ state: 'visible', timeout: 10000 }).catch(() => {});
  1460 |             await settle(page, 1000);
  1461 |             Logger.info('Step 2 ✓: Document unarchived');
  1462 |         } else {
  1463 |             await page.keyboard.press('Escape');
  1464 |             Logger.info('Step 2: Unarchive not available — using Assign from archived state');
  1465 |         }
  1466 | 
  1467 |         // Step 3: Assign the document to patient (ensure Show Archived is unchecked)
  1468 |         await page.locator("(//*[contains(text(),'Document Library')])[1]").first().click();
  1469 |         await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  1470 |         await settle(page, 1500);
  1471 |         await docPage.clickFolder('Education');
  1472 |         await settle(page, 2000);
  1473 | 
  1474 |         const archiveCheckbox = page.locator("//input[@type='checkbox']").first();
  1475 |         if (await archiveCheckbox.isChecked().catch(() => false)) {
  1476 |             await docPage.toggleShowArchived();
  1477 |             await settle(page, 2000);
  1478 |         }
  1479 | 
  1480 |         await docPage.searchDocument(fn);
  1481 |         await docPage.assignDocumentToPatient(0, patient.lastName);
  1482 |         Logger.info('Step 3 ✓: Document assigned to Automation, Test');
  1483 | 
  1484 |         // Step 4: Verify in patient charting
  1485 |         const patientChart = new PatientChartPage(page);
  1486 |         await patientChart.openPatientChartFromGlobalSearch(patient.displayName);
  1487 |         await settle(page, 2000);
  1488 |         await page.locator("button:has-text('CCM')").first().or(page.getByText('CCM', { exact: true }).first()).click();
  1489 |         await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  1490 |         await settle(page, 2000);
  1491 |         await page.locator("button:has-text('Documents')").first().click();
  1492 |         await settle(page, 2000);
  1493 | 
  1494 |         const educationFolder = page.locator("*:has-text('Education')").filter({ hasText: /^Education$/ });
  1495 |         if (await educationFolder.count() > 0) {
  1496 |             await educationFolder.first().click();
  1497 |             await settle(page, 2000);
  1498 |         }
  1499 | 
  1500 |         // Auto-retry until the unarchived+assigned document renders in the folder.
  1501 |         await expect(page.locator('main'),
  1502 |             `Unarchived document "${fn}" should appear in patient charting`)
> 1503 |             .toContainText(fn, { timeout: 15000 });
       |              ^ Error: Unarchived document "UnarcAssign 1787561240431" should appear in patient charting
  1504 | 
  1505 |         Logger.info(`AUT_DOC_022 PASSED: Unarchived document assigned and verified in patient chart`);
  1506 |     });
  1507 | });
  1508 | 
  1509 | // ============================================================
  1510 | // === document-unsupported-format.spec.ts
  1511 | // ============================================================
  1512 | test.describe('Document - Unsupported Format Validation', () => {
  1513 |     let loginPage: ProviderLoginPage;
  1514 |     let docPage: DocumentLibraryPage;
  1515 |     const { providerCredentials, providerPortalUrl } = getEnvironmentConfig();
  1516 | 
  1517 |     test.beforeEach(async ({ page }) => {
  1518 |         loginPage = new ProviderLoginPage(page);
  1519 |         docPage = new DocumentLibraryPage(page);
  1520 |         await page.goto(providerPortalUrl);
  1521 |         await loginPage.login(providerCredentials.username, providerCredentials.password);
  1522 |         await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  1523 |     });
  1524 | 
  1525 |     test('AUT_DOC_008 - Verify unsupported file format validation (.xlsx rejected)', async ({ page }) => {
  1526 |         test.setTimeout(120000);
  1527 |         await docPage.navigateToDocumentLibrary();
  1528 |         await docPage.clickUploadMaterial();
  1529 |         await docPage.selectTag('Education');
  1530 |         await docPage.selectSpeciality('Cardiologist');
  1531 |         await docPage.fillFileName('Unsupported File');
  1532 |         await docPage.uploadFile(`${docsPath}/4mb.xlsx`);
  1533 |         await settle(page, 2000);
  1534 | 
  1535 |         const errorMsg = await docPage.getFileFormatError();
  1536 |         const state = await docPage.getUploadValidationState();
  1537 |         expect(state.buttonDisabled || errorMsg.length > 0, 'Unsupported format should be rejected').toBeTruthy();
  1538 |         Logger.info(`AUT_DOC_008 PASSED: .xlsx rejected. Error: "${errorMsg}"`);
  1539 |     });
  1540 | });
  1541 | 
  1542 | // ============================================================
  1543 | // === document-upload.spec.ts
  1544 | // ============================================================
  1545 | test.describe('Document Library - Upload', () => {
  1546 |     let loginPage: ProviderLoginPage;
  1547 |     let docPage: DocumentLibraryPage;
  1548 |     const { providerCredentials, providerPortalUrl } = getEnvironmentConfig();
  1549 | 
  1550 |     test.beforeEach(async ({ page }) => {
  1551 |         loginPage = new ProviderLoginPage(page);
  1552 |         docPage = new DocumentLibraryPage(page);
  1553 | 
  1554 |         Logger.step('Login to Provider Portal');
  1555 |         await page.goto(providerPortalUrl);
  1556 |         await loginPage.login(providerCredentials.username, providerCredentials.password);
  1557 |         await page.waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {});
  1558 |     });
  1559 | 
  1560 |     test('AUT_DOC_001 - E2E Document Upload - Upload to Education folder with all fields, verify in correct folder', async ({ page }) => {
  1561 |         test.setTimeout(300000);
  1562 |         Logger.step('Test: E2E Document Upload Workflow');
  1563 | 
  1564 |         // Step 1: Navigate to Document Library
  1565 |         await docPage.navigateToDocumentLibrary();
  1566 |         const isOnPage = await docPage.verifyOnDocumentLibrary();
  1567 |         expect(isOnPage, 'Should be on Document Library page').toBeTruthy();
  1568 |         Logger.info('Step 1 ✓: Navigated to Document Library');
  1569 | 
  1570 |         // Step 2: Verify all folders are visible
  1571 |         const folders = await docPage.getFolderNames();
  1572 |         expect(folders).toContain('Home Exercise');
  1573 |         expect(folders).toContain('Education');
  1574 |         expect(folders).toContain('Challenges');
  1575 |         expect(folders).toContain('Care Plan');
  1576 |         expect(folders).toContain('Consent');
  1577 |         Logger.info(`Step 2 ✓: All 5 folders visible: ${folders.join(', ')}`);
  1578 | 
  1579 |         // Step 3: Click Education folder
  1580 |         await docPage.clickFolder('Education');
  1581 |         await page.screenshot({ path: 'test-results/doc-education-before-upload.png', fullPage: true });
  1582 |         Logger.info('Step 3 ✓: Education folder opened');
  1583 | 
  1584 |         // Step 4: Upload document with all required fields
  1585 |         const timestamp = Date.now();
  1586 |         const fileName = `AutoDoc ${timestamp}`;
  1587 |         const description = `Automated upload test at ${new Date().toISOString()}`;
  1588 | 
  1589 |         await docPage.uploadDocument({
  1590 |             tag: 'Education',
  1591 |             speciality: 'Cardiologist',
  1592 |             fileName,
  1593 |             // Use the fixtures-resolved docsPath so this works on CI as well
  1594 |             // as locally; the hardcoded user-home path was developer-local.
  1595 |             filePath: `${docsPath}/4-mb-example-file (1).pdf`,
  1596 |             description
  1597 |         });
  1598 |         Logger.info(`Step 4 ✓: Document "${fileName}" uploaded with tag=Education, speciality=Cardiologist`);
  1599 | 
  1600 |         // Step 5: Navigate back to Document Library and open Education folder
  1601 |         await docPage.navigateToDocumentLibrary();
  1602 |         await docPage.clickFolder('Education');
  1603 |         await settle(page, 2000);
```